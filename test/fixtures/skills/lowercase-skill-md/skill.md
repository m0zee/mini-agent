---
name: lowercase-skill-md
description: A skill stored in a file literally named skill.md, exercising the lowercase-filename fallback in discovery.
---

This fixture's frontmatter matches its directory name exactly and should
load with zero diagnostics via the skill.md fallback.
